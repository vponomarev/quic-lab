server {
    listen 80;
    listen [::]:80;
    server_name quic.example.org;
    root /var/www/quic-demo;
    location /.well-known/acme-challenge/ { try_files $uri =404; }
    location / { default_type text/plain; return 200 "QUIC Lab setup\n"; }
}
